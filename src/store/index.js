import { createStore } from 'vuex';
import transactionData from './transactionData';

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    transactionData,
  },
});
